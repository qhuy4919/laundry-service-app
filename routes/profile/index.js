const profile_route = require('./profile');
const follower_route = require('./follower');
const total_orders_route = require('./total-orders');
const order_list_route = require('./order-list');

module.exports = {
	profile_route,
	follower_route,
	total_orders_route,
	order_list_route,
}